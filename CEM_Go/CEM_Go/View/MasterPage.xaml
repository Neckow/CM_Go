<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="CEM_Go.View.MasterPage"
              Padding="0,40,0,0"
			        Icon="hamburger.png"
              Title="Personal Organiser">
  <ContentPage.Content>
    <StackLayout VerticalOptions="FillAndExpand">
      <ListView x:Name="listView" VerticalOptions="FillAndExpand" SeparatorVisibility="Default" RowHeight="70" IsVisible="True" >
          <ListView.Header>
              <StackLayout Padding="10,5,0,5" BackgroundColor="#cccccc">
                  <Label Text="Header" />
                  <Label Text="Subhead" FontSize="Small" />
              </StackLayout>
          </ListView.Header>
          <!-- Doesn't work 
          <ListView.HeaderTemplate> 
              <DataTemplate>
                  <StackLayout Orientation="Horizontal">
                      <Label Text="HEADER"/>
                  </StackLayout>
              </DataTemplate>
          </ListView.HeaderTemplate>-->
        <ListView.ItemTemplate>
          <DataTemplate>
            <ImageCell Text="{Binding Title}" ImageSource="{Binding IconSource}" />
          </DataTemplate>
        </ListView.ItemTemplate>
      </ListView>
    </StackLayout>
  </ContentPage.Content>
</ContentPage>